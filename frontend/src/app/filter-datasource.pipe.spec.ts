import { FilterDatasourcePipe } from './filter-datasource.pipe';

describe('FilterDatasourcePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDatasourcePipe();
    expect(pipe).toBeTruthy();
  });
});
